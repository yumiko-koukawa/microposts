<span>
  <% if current_user == micropost.user %>
  <% elsif micropost.original_user.nil? %>
    <%= link_to t('views.microposts.favorite'), favorite_micropost_path(micropost),
                         method: :post,
                         data: { confirm: "お気に入り登録しますか?" } %>
  <% else %>
    favorite済み
  <% end %>
</span>